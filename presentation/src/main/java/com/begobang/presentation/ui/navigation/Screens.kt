package com.begobang.presentation.ui.navigation

/*
    This class will provide with the different screens parents that have our application.
    For example, we could add every feature that provides a bottom bar.
 */
enum class Screens(val route: String) {
    POKEMON(route = "pokemons")
}